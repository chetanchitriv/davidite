import { AdComponentDirective } from './ad-component.directive';

describe('AdComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new AdComponentDirective();
    expect(directive).toBeTruthy();
  });
});
